public enum PlayerState
{
    Idle,
    Jump,
    Cheer,
    Lose
}

public enum CardinalDirection
{
    Directionless,
    Forward,
    Right,
    Backward,
    Left,
    Unknown
}

public enum PusherState
{
    Idle,
    Bounce
}